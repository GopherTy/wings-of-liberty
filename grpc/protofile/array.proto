syntax = "proto3";
package  grpc.protofile.array;

service Array {
    rpc GetEncryptionArray (ArrayRequest) returns (ArrayResponse);
} 

message ArrayRequest{
}

message ArrayResponse{
    bytes array = 1; 
}